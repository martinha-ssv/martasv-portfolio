import React from 'react';

export default function NavBar() {
    return (
        <div className="flex rounded-sm border-white/30 hover:border-white/50">
            
        </div>
    );
}

function NavBarIcon({ iconUrl, target }) {
    
}